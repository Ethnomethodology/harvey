--- TableViewerPanel.svelte
+++ TableViewerPanel.svelte
@@ -2741,7 +2741,13 @@
             tabulatorInstance.on("cellEditing", handleCellEditing);
             tabulatorInstance.on("cellEditCancelled", handleCellEditCancelled);
-            tabulatorInstance.on("columnResized", saveCurrentTableLayout);
+            tabulatorInstance.on("columnResized", (column) => {
+                saveCurrentTableLayout();
+                // Recalculate row heights after column resize to wrap text instantly
+                if (tabulatorInstance) {
+                    tabulatorInstance.rowManager.normalizeHeightToFitData();
+                }
+            });
             tabulatorInstance.on("columnMoved", saveCurrentTableLayout);
             // Setup initial rows for keyboard shortcuts
