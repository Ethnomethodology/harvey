// src/lib/constants/windowSize.js

/**
 * Shared window dimensions for the Harvey application.
 */
export const PROJECT_MIN_WIDTH = 1024;
export const WELCOME_WIDTH = 1024;
export const WELCOME_HEIGHT = 800; // Fixed height as per user preference
export const DEFAULT_MIN_HEIGHT = 600;
