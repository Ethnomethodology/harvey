name: Test CI Environment

on:
  workflow_dispatch:

jobs:
  test-environment:
    runs-on: windows-latest
    steps:
      - name: Checkout application code
        uses: actions/checkout@v4

      - name: Set target triple
        id: set_target
        run: |
          echo "triple=x86_64-pc-windows-msvc" >> $GITHUB_OUTPUT
        shell: bash

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: ${{ steps.set_target.outputs.triple }}
          override: true

      - name: Set up MSVC Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1

      - name: Set up MSYS2 (MINGW64 for x64)
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            make
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-gcc
            mingw-w64-x86_64-yasm
            mingw-w64-x86_64-nasm
            mingw-w64-x86_64-pkg-config

      - name: Add MSYS2 to PATH
        run: |
          echo "C:\\msys64\\mingw64\\bin" >> $GITHUB_PATH
          echo "C:\\msys64\\usr\\bin" >> $GITHUB_PATH

      - name: Run Diagnostics
        run: |
          echo "--- PowerShell PATH ---"
          echo $env:PATH
          echo "--- Checking for executables in PowerShell ---"
          where.exe bash
          where.exe cargo
          where.exe cl
          where.exe lib
          echo "--- Running diagnostics within Bash (with PATH conversion) ---"
          bash -c 'echo "--- Original Bash PATH ---"; echo $PATH; export PATH=$(cygpath -p "$PATH"); echo "--- Converted Bash PATH ---"; echo $PATH; echo "--- Checking for executables in Bash (after conversion) ---"; which cargo; which cl; which lib; which bash'
